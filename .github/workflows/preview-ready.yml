name: Preview Ready

# Manages the `preview-ready` label on pull requests. The label tells Flux
# ResourceSetInputProvider (in k8s-config) to provision a preview environment.
#
# Triggered by:
#   - pull_request_target (open/reopen/sync): clears stale label so a fresh
#     image build must succeed before the label is re-added.
#   - workflow_run (CI completed): if docker-pr succeeded AND the image exists
#     in the registry AND the head SHA matches the PR tip, re-add the label.
#
# Same pattern as rezuscloud/platform-website/.github/workflows/preview-ready.yml.

on:
  pull_request_target:
    types: [opened, reopened, synchronize]
  workflow_run:
    workflows: ["CI"]
    types: [completed]

permissions:
  actions: read
  contents: read
  issues: write
  pull-requests: write

jobs:
  clear:
    if: github.event_name == 'pull_request_target' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Ensure gh CLI
        run: which gh || (curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null && sudo apt-get update -qq && sudo apt-get install -y -qq gh)

      - name: Remove preview-ready label
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: gh api --silent --method DELETE repos/${{ github.repository }}/issues/${PR_NUMBER}/labels/preview-ready || true

  mark:
    if: github.event_name == 'workflow_run'
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Ensure gh CLI
        run: which gh || (curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null && sudo apt-get update -qq && sudo apt-get install -y -qq gh)

      - name: Mark preview ready after successful docker-pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_ID: ${{ github.event.workflow_run.id }}
        run: |
          set -euo pipefail

          run_payload="$(gh api repos/${{ github.repository }}/actions/runs/${RUN_ID})"
          pr_number="$(jq -r '.pull_requests[0].number // empty' <<< "$run_payload")"
          head_sha="$(jq -r '.head_sha' <<< "$run_payload")"

          if [ -z "$pr_number" ] || [ "$pr_number" = "null" ]; then
            echo "No PR context for workflow run $RUN_ID, skipping"
            exit 0
          fi

          # Bail out if the PR has moved on since the workflow ran.
          current_head_sha="$(gh pr view "$pr_number" --repo "${{ github.repository }}" --json headRefOid --jq '.headRefOid')"
          if [ "$current_head_sha" != "$head_sha" ]; then
            gh api --silent --method DELETE repos/${{ github.repository }}/issues/${pr_number}/labels/preview-ready || true
            echo "HEAD SHA mismatch (current=$current_head_sha, run=$head_sha), cleared label"
            exit 0
          fi

          docker_pr_conclusion="$(gh api repos/${{ github.repository }}/actions/runs/${RUN_ID}/jobs --paginate --jq '.jobs[] | select(.name == "Build and push PR image") | .conclusion' | tail -n 1)"

          if [ "$docker_pr_conclusion" != "success" ]; then
            gh api --silent --method DELETE repos/${{ github.repository }}/issues/${pr_number}/labels/preview-ready || true
            echo "docker-pr job did not succeed ($docker_pr_conclusion), cleared label"
            exit 0
          fi

          if ! docker buildx imagetools inspect "ghcr.io/${{ github.repository }}:pr-${pr_number}-${head_sha}" 2>/dev/null; then
            echo "Image pr-${pr_number}-${head_sha} not found in registry, skipping"
            exit 0
          fi

          gh api --silent --method POST repos/${{ github.repository }}/issues/${pr_number}/labels -f labels[]=preview-ready
          echo "Added preview-ready label to PR #${pr_number}"
